{
  "installCommand": "echo \"Migration page only — skip install\"",
  "buildCommand": "echo \"Migration page only — skip build\"",
  "outputDirectory": "vercel-legacy",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
